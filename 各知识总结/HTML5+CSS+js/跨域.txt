跨域请求：
	跨域请求：当前发起请求的域（或服务器）与该请求指向的资源所在域不一致（这里所有的域是协议，域名和端口号的合集，同域就是协议，域名和端口号都相同，任何一个不同都是跨域）

	1.由于浏览器的安全限制，不允许ajax跨域访问，浏览器认为这种访问不安全
	2.可以通过动态生成script标签的形式，把script标签的src属性指向接口的地址，因为script标签不存在跨域问题，这种获取数据的方式叫做JSONP