基于XML配置:
	1.Bean实现类来源于第三方类库，如 DataSource,JdbcTemplate等，无法在类中标注注解，所以通过XML配置方式比较好。
	2.命名空间的配置，如果aop,context等,只能采用基于XML的配置方式。


基于注解配置:
	1.Bean的实现类是当前项目开发的，可以直接在Java类中使用基于注解的配置。


基于Java类配置:
	1.基于Java类配置的优势在于可以通过代码方式控制Bean初始化的整体逻辑。如果实例化Bean的逻辑比较复杂，则基于Java类配置更合适。
